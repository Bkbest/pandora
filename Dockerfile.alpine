FROM alpine

# Install system dependencies including Python, pip, numpy, matplotlib and pandas
RUN apk add --no-cache python3 py3-pip python3-matplotlib python3-numpy python3-pandas curl nodejs npm bash git msmtp

# Create workspace and packages directories
RUN mkdir -p /workspace/.packages

# Set Python to use UTF-8 and default pip target
ENV PYTHONIOENCODING=utf-8 \
    PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_TARGET=/workspace/.packages \
    PYTHONPATH=/workspace/.packages

# Create a workspace directory
WORKDIR /workspace

# Default command
CMD ["python"]
